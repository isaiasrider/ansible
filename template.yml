---
 - hosts: all
   tasks:
     - name: 'Faz a instalação de alguns pacotes'
       apt:
         name: "{{ packages }}"
         state: latest
       become: yes
       vars:
         packages:
         #liste os pacotes abaixo
         #- pacote example
         #- pacote example

       #- debug:
           msg: "{{ variavel_especial }}"

       #- meta: end_play